% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pca_check.R
\name{pca_check}
\alias{pca_check}
\title{Check PCA Data Range}
\usage{
pca_check(df, var_names = NULL, digits = 2)
}
\arguments{
\item{df}{A data frame containing the variables for PCA analysis}

\item{var_names}{Optional character vector specifying the names of variables to be used in PCA.
If NULL, all numeric columns except 'Group' and 'SampleID' will be used.}

\item{digits}{Number of decimal places to round the results (default: 2)}
}
\value{
A list containing the ranges of PC1 and PC2, and prints the ranges to console
}
\description{
This function checks the range of PC1 and PC2 in PCA analysis to help set appropriate plot limits.
}
\examples{
\dontrun{
# Create example data
df_example <- data.frame(
  SampleID = paste0("Sample_", 1:10),
  Group = rep(c("A", "B"), each = 5),
  Var1 = rnorm(10),
  Var2 = rnorm(10)
)

# Check PCA ranges
ranges <- pca_check(df_example)
}
}
